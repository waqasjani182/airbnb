import 'package:flutter/material.dart';

class RatePendingScreen extends StatelessWidget {
  const RatePendingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Rate Pending")),
    );
  }
}